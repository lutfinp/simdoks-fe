import Hasdok from "./Hasdok";

const Hapus = ({ data }) => {
  return (
    <div className="ml-[32px] mr-[32px] my-4">
      <div>
        <Hasdok data={data}/>
      </div>
    </div>
  );
};

export default Hapus;
