import React from "react";
import Allfolder from "./Allfolder";

const ListFolder = ({ data, id }) => {
  return (
    <>
      <Allfolder data={data} id={id}/>
    </>
  );
};

export default ListFolder;
